language: node_js

node_js:
  - "10"

install:
  - npm install

script:
  - npm run build

after_script:
  # Run the deployment script only for new commits
  # on the master branch.
  - test $TRAVIS_BRANCH = "master" && ./deploy.sh
